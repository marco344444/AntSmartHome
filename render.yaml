services:
  - type: web
    name: antsmarthome
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: WHATSAPP_NUMBER
        value: "573001234567"
